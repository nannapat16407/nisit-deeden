apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: default
  labels:
    app: frontend
    env: uat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
        env: uat
    spec:
      containers:
      - name: frontend
        # Local: kuis-frontend:uat  |  CI: ghcr.io/.../kuis-frontend:uat-latest
        image: kuis-frontend:uat
        imagePullPolicy: Never
        ports:
        - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: default
spec:
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000

---
# Ingress: 2 hosts per env (align with manifest)
# - Public link: uat-kuis-award.local (app)
# - API link:    api.uat-kuis-award.local (backend)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  rules:
  # App host: /api -> backend (for OAuth callback /api/auth/session), / -> frontend
  - host: uat-kuis-award.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
  # API host: all paths -> backend (for frontend API calls to api.uat-kuis-award.local)
  - host: api.uat-kuis-award.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 80
